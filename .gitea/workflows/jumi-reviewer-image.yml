name: Build Jumi Reviewer Image

on:
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  # false: avoid cancelled mid-build steps reporting oddly green on Gitea act.
  cancel-in-progress: false

jobs:
  image:
    # GARM K8s runners (Buildah baked into garm-act-runner). Not Akka/Kaniko.
    runs-on: ubuntu-latest
    env:
      IMAGE: gitea.kirmanak.stream/personal/jumi-reviewer
      REGISTRY: gitea.kirmanak.stream

    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          fetch-depth: 0

      - name: Load tool versions
        run: grep -v '^#' .gitea/tool-versions.env >> $GITHUB_ENV

      - name: Set up Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: ${{ env.BUN_VERSION }}

      - name: Install dependencies
        run: bun install --frozen-lockfile
        working-directory: scripts/opencode

      - name: Run lint, typecheck, and tests
        run: bun run ci
        working-directory: scripts/opencode

      - name: Compute version
        run: echo "VERSION=$(bun src/release.ts next-version)" >> $GITHUB_ENV
        working-directory: scripts/opencode

      - name: Build and push image
        env:
          PUSH_IMAGE: "true"
          CONTAINER_REGISTRY_USER: ${{ vars.CONTAINER_REGISTRY_USER }}
          CONTAINER_REGISTRY_PASS: ${{ secrets.CONTAINER_REGISTRY_PASS }}
          COMMIT_SHA: ${{ github.sha }}
          REVISION: ${{ github.sha }}
          REF_NAME: ${{ github.ref_name }}
          REF_TYPE: ${{ github.ref_type }}
        run: |
          tags=("${IMAGE}:latest" "${IMAGE}:${VERSION}")
          bash .gitea/scripts/build-reviewer-image.sh "${tags[@]}"
