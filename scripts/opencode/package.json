{
  "name": "opencode-gitea",
  "version": "1.0.0",
  "description": "OpenCode AI agent orchestration for Gitea",
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "server": "bun run src/server.ts",
    "lint": "biome check src test package.json tsconfig.json ../../.gitea/opencode-review.json",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "typecheck": "tsc --noEmit",
    "ci": "bun run lint && bun run typecheck && bun test"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.1",
    "@types/bun": "1.4.0",
    "typescript": "7.0.2"
  }
}
